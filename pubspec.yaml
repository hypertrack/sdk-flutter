name: hypertrack_plugin
description: A new flutter plugin project.
version: 0.1.1
homepage: https://www.hypertrack.com/

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: com.hypertrack.sdk.flutter
    pluginClass: HyperTrackPlugin
